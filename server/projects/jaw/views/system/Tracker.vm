<script type="text/javascript" src="/jaw/scripts/system.js"></script>
{{{ SYSTEM_MENU }}}
{{{ SHOW_REQUEST }}}
{{{ REGISTER_TICKET }}}
<div class="system-container">
	<br><br><br>
	#if (!$this.getSession().getParms().containsKey("action"))
        {{{ TRACKER_COMPANY }}}
	#elseif ($this.GET("action").equals("current"))
        {{{ TRACKER_CURRENT }}}
	#elseif ($this.GET("action").equals("company"))
        {{{ TRACKER_COMPANY }}}
	#elseif ($this.GET("action").equals("project"))
        {{{ TRACKER_PROJECT }}}
	#end
	#if ($this.checkAccess("ticket/register"))
		<button class="btn btn-primary" data-toggle="modal" data-target="#register-ticket">Создать задачу</button>
		$this.runWidget("Form", "{ form: 'Ticket', id: 'register-ticket-form', title: 'Добавить задачу' }")
		<br>
        {{{ FORM }}}
	#end
</div>